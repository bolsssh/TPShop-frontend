import generatePredicateForOrderUnique from 'ember-app/utils/generate-predicate-for-order-unique';
import { module, test } from 'qunit';

module('Unit | Utility | generate-predicate-for-order-unique', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = generatePredicateForOrderUnique();
    assert.ok(result);
  });
});
